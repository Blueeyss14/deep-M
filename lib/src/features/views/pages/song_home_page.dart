import 'package:flutter/material.dart';

class SongHomePage extends StatelessWidget {
  const SongHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Ini adalah Song Page")));
  }
}
